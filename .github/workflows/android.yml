name: Build Android

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  build:
    name: Build Android
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Update Submodules (Mihomo Kernel)
        run: |
          git submodule update --remote --recursive
          cd core/src/foss/golang/clash
          git checkout Alpha || git checkout Prerelease-Alpha || git checkout main
          git pull origin Alpha || git pull origin Prerelease-Alpha || git pull origin main

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27b
          add-to-path: true

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew assembleMetaDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: clash-meta-android-debug
          path: app/build/outputs/apk/meta/debug/*.apk
          retention-days: 30
